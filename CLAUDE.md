@AGENTS.md

Claude-specific note: this repo's attribution settings live in `.claude/settings.json`.
Commits and PR descriptions should carry no AI attribution trailer — see the Git/commits
section in AGENTS.md above. If a future session ever shows a `Co-Authored-By: Claude` line,
the settings file below has been overridden or is missing; restore it before committing.
